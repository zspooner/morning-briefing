name: Keepalive

on:
  schedule:
    # Run every Sunday at midnight UTC to keep repo active
    - cron: '0 0 * * 0'

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Keep workflow alive
        run: echo "Keeping scheduled workflows active - $(date)"
